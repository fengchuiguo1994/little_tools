<rules>
<rule>
# raise the limit to 2000
condition  = var(value) < 1000
# change this rule so that these
# elements are hidden (show=no)
color      = black
</rule>
<rule>
# raise the limit to 6000
condition  = var(value) < 2000
# make this grey lighter
color      = grey
</rule>
</rules>