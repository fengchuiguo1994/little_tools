
<links>

<link>
file          = ../data/links.txt
bezier_radius = 0r
radius        = 0.5r
thickness     = 1p
color         = black_a5

<rules>

use         = no

<rule>
use         = no
condition   = on(hs1)
color       = rdylbu-11-div-2_a3
</rule>

<rule>
use         = no
condition   = between(hs1,mm1)
#condition   = between(hs1,mm1) || between(hs2,mm2) 
condition   = var(start2) < 40mb || var(start2) > 160mb
thickness   = eval(remap_round(var(size1),0,50000,1,5))
z           = eval(remap_round(var(size1),0,50000,1,5))
color       = eval(sprintf("rdylbu-11-div-%d_a3",remap_round(var(start1),0,250e6,1,11)))
</rule>

<rule>
condition  = 1
show       = no
</rule>

</rules>

</link>

</links>

<plots>
<<include ../2/etc/histograms.conf>>
<<include ../3/etc/heatmaps.conf>>
<<include ../5/etc/histograms.conf>>
<<include ../6/etc/tiles.conf>>
<<include ../7/etc/highlights.conf>>
</plots>

<<include ../etc/karyotype.and.layout.conf>>

<<include ../etc/ideogram.conf>>

<<include ../etc/ticks.conf>>

<<include ../../etc/image.conf>>

<<include etc/colors_fonts_patterns.conf>>

<<include etc/housekeeping.conf>>
